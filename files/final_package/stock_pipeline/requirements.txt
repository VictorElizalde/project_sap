# SAP HANA → Power BI Pipeline — Python dependencies
# Install with:  pip install -r requirements.txt

# SAP HANA Python driver (official)
hdbcli>=2.19.0

# Data manipulation
pandas>=2.0.0
numpy>=1.24.0

# HTTP client (Power BI REST API)
requests>=2.31.0

# Environment variable loading from .env file
python-dotenv>=1.0.0

# Optional: progress bars for large data pushes
tqdm>=4.65.0
